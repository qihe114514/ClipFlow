# ClipFlow v4.0

> 质量与体验大版本：围绕解析、下载、隐私合规、无障碍与性能做了一次系统性修复，APK 体积缩减 85%。

## 下载与保存

- 修复“保存路径”设置不生效：自定义目录（SAF）现在真正控制视频/图片/音乐落盘位置，目录不可用时明确报错，不再静默回退默认目录
- 下载完成写入系统相册的操作移出主线程，消除大文件下载完成瞬间的卡死与 ANR 风险
- 新增下载前台服务：Android 12+ 退到后台时下载不再被系统冻结中断
- 新增下载进度/完成通知；Android 13+ 在首次下载时请求通知权限，拒绝也不影响下载
- 支持取消下载；下载完成后按媒体类型正确打开；列表项显示“已保存”，避免重复下载

## 解析与 B 站

- B 站支持 HEVC 视频流（同一清晰度优先 AVC，兼容性最好），清晰度列表显示实际编码
- B 站播放/音频地址统一使用 HTTPS；登录会话改为内存缓存
- 扫码登录二维码改为后台生成，轮询间隔调整为 2 秒，降低卡顿与耗电
- 小红书结果类型徽章修正；多分 P 标签与实际选择一致
- 支持从系统分享面板直接把抖音/小红书/B 站链接分享到 ClipFlow 并自动解析

## 隐私与合规

- 同意隐私政策前不再发起任何网络请求（含更新检查），友盟初始化后移到同意之后
- “关于 → 隐私”撤回同意会真正通知友盟停止采集，文案与入口一致
- 隐私政策补全第三方解析接口、B 站登录、剪贴板与本地数据说明，政策链接可点击
- 更新包增加 SHA-256 + 签名双重校验，校验失败自动删除
- 备份规则排除本机加密的 B 站会话，避免跨设备恢复产生无效数据

## 体验与无障碍

- 修复新手引导中残留的调试数字“2193”
- 顶栏浅色模式图标对比度、强制深色下的底栏/滑杆配色、Tab 选中语义、按钮禁用态视觉
- 系统大字体下特性卡片与登录弹窗不再截断
- 历史搜索防抖、导入去重、跨年日期分组修正、LIKE 通配符转义
- 设置页新增“存储 → 下载缓存”查看与清理

## 性能与体积

- APK 59.5MB → 约 8.6MB：启动图标按密度重新生成，内置壁纸转换为 JPEG
- 网络请求共享连接池；幂等 GET 请求在弱网/瞬时 5xx 时自动重试一次
- 下载通知 500ms 节流，二维码/模糊等重活移出主线程

## 工程

- kapt 迁移到 KSP，兼容 Kotlin 2.3 / AGP 9.1
- 单元测试 46 → 66 条；Lint 0 Error

版本号：`4.0`（versionCode `90`），最低支持 Android 10。
